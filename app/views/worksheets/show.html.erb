<% content_for :title, "#{@skill.title} — worksheet" %>

<% unless @bare %>
  <div class="toolbar">
    <button class="print" onclick="window.print()">Print / Save as PDF</button>
    <%= link_to "Generate new practice", @regen_url, class: "regen" %>
    <%= link_to "← Back", worksheet_studio_path(@skill.id), class: "back" %>
  </div>
<% end %>

<div class="sheet">
  <header class="sheet-header">
    <h1><%= @skill.title %></h1>
    <p><%= @activity.title %></p>
  </header>

  <div class="sheet-body">
    <%= render "worksheets/templates/#{@worksheet[:template]}", worksheet: @worksheet %>
  </div>

  <footer class="sheet-footer">
    <span>Name: <strong><%= @child.name %></strong></span>
    <span><%= @skill.title %></span>
    <span><%= Date.current.strftime("%-d %B %Y") %></span>
  </footer>
</div>
